% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/sankey_utils.R
\name{sankey_prepare}
\alias{sankey_prepare}
\title{Prepare trajectory data from multi-temporal classification cube}
\usage{
sankey_prepare(
  cube,
  years,
  output_dir,
  multicores = 10,
  memsize = 16,
  roi = NULL
)
}
\arguments{
\item{cube}{A \code{sits} classification cube tibble where each row
represents one time step.}

\item{years}{A numeric or character vector of year labels, one per
cube row. Used as column names in the output.}

\item{output_dir}{Character path for intermediate files.}

\item{multicores}{Integer number of cores for parallel processing
(default: 10).}

\item{memsize}{Integer max memory in GB (default: 16).}

\item{roi}{Optional region of interest (ROI) to filter chunks.}
}
\value{
A tibble with one column per year and a \code{freq} column with
pixel counts for each unique trajectory.
}
\description{
Reads a classification cube and computes pixel
trajectory frequencies using chunk-based parallel processing. The output
is a trajectory frequency table suitable for Sankey diagrams, temporal
analysis, or transition studies.

Each row in the result represents a unique trajectory (sequence of class
labels across time steps), with the \code{freq} column indicating how many
pixels followed that trajectory.
}
\note{
All cube rows must share the same spatial grid (extent, resolution,
and CRS). The function validates grid alignment before processing.
}
\author{
Felipe Carlos, \email{efelipecarlos@gmail.com}

Felipe Carvalho, \email{felipe.carvalho@inpe.br}
}
