% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/val_crosstable.R
\name{crosstable_area}
\alias{crosstable_area}
\title{Cross-tabulation of area between two categorical rasters}
\usage{
crosstable_area(
  map,
  ref,
  output_dir,
  unit = "ha",
  multicores = 10,
  memsize = 16,
  roi = NULL
)
}
\arguments{
\item{map}{A \code{sits} cube with the classification map.}

\item{ref}{A \code{sits} cube with the reference map.}

\item{output_dir}{Character specifying the output directory for
intermediate files.}

\item{unit}{Character specifying the area unit. One of
\code{"ha"} (hectares, default), \code{"km2"}
(square kilometers), or \code{"m2"} (square meters).}

\item{multicores}{Integer specifying the number of cores for parallel
processing (default: 10).}

\item{memsize}{Integer specifying the maximum memory size in GB
(default: 16).}

\item{roi}{Optional region of interest (ROI) to filter chunks.}
}
\value{
A tibble with columns \code{map_values}, \code{ref_values},
and \code{area} (in the specified unit).
}
\description{
Computes the area of intersecting pixels between two
categorical rasters. This is similar to \code{\link{crosstable}}, but
returns area instead of pixel counts. Area is computed using
\code{\link[raster]{area}}, which accounts for cell size variations
in geographic coordinate systems.
}
\author{
Felipe Carlos, \email{efelipecarlos@gmail.com}

Felipe Carvalho, \email{felipe.carvalho@inpe.br}
}
